{
  "name": "demo-app",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal TodoMVC fixture for localsprite smoke tests",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=web\"",
    "dev:server": "npm run dev --workspace=server",
    "dev:web": "npm run dev --workspace=web"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
